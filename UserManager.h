#ifndef USERMANAGER_H
#define USERMANAGER_H

#include "StorageManager.h"

class UserManager
{
public:
    UserManager();
    createProfile(data);
    deleteProfile();
    updateProfile(data);
    login(credentials);
    logout();
private:
    
};

#endif // USERMANAGER_H
